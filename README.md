I2C_SUBSYSTEM:
-------------
1. Run the shell script [ pwd @ SOC-Integration-Structure/SCRIPT] by command : source source_file.sh 
2. after the script is executed check : echo $REPO_ROOT [It should be assigned to[ pwd @ SOC-Integration-Structure] ].
3. Then run the make file for executing the testcases.
